version: '3.8'

services:
  inwork-api:
    build:
      context: .
      dockerfile: apps/api-server/Dockerfile
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=MVP-PRODUCTION
      - PORT=3000
    healthcheck:
      test: ["CMD", "node", "-e", "fetch('http://localhost:3000/health').then(r => r.ok ? process.exit(0) : process.exit(1))"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 10s
    restart: always
